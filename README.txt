MinMaxMeshReader:
 -> Writes attribute files and points files (.lpts) from a mesh file corresponding to min and max points for each direction.
 -> Tool used for the particle correspondence preprocessing to have initialization points
